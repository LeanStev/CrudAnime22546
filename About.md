//Este directorio pertece al proyecto realizado para el programa "Codo a Codo". 
//En el mismo se realizo un CRUD realizando la conexion con una base de datos MySQL.
//Se creo un frontend con archivos JSP (Bootstrap,HTML,CSS,JS) para que el usuario pueda interactuar con el mismo.
//Se realizo el backend las peticiones correspodientes con lenguaje JAVA.
